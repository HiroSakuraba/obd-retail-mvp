// Root build: shared config only. All testable logic lives in :core
// (pure Kotlin/JVM). The full Android :app module (Manifest, Compose,
// real BLE GATT) is future work requiring the Android SDK — see README.md.
